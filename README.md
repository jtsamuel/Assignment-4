# Assignment4

To run the website, run the following while in the directory:
```bash
python3 -m http.server
```

If the `python3` command is not available on your system, use:

```bash
python -m http.server
```

Or use the VSCode "Live Server" extension, which should automatically open the application in your browser.
